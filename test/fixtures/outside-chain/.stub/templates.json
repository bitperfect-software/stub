{
    "variables": [{ "name": "entity", "description": "the entity name" }],
    "reference": "@/{{ targetPath | remove_first: 'out/' }}",
    "templates": {
        "model": { "path": "out/{{ entity }}.ts" },
        "sibling": { "path": "out/{{ entity }}Sibling.ts" }
    }
}
